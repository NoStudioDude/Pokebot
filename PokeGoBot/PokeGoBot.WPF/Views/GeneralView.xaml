<UserControl x:Class="PokeGoBot.WPF.Views.GeneralView"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokeGoBot.WPF.Views"
             mc:Ignorable="d" d:DesignHeight="320" d:DesignWidth="742">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourcesDictionary/metroApp.xaml" />
                <ResourceDictionary Source="../ResourcesDictionary/Styles/MainStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel HorizontalAlignment="Left" Orientation="Vertical" Margin="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource TitleTextBlock}" 
                           Text="Google" Margin="0,5,0,5" Width="100" />

                    <Controls:ToggleSwitchButton IsChecked="{Binding UseGoogle}" />

                </StackPanel>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource TitleTextBlock}" 
                           Text="Username" Margin="0,5,5,5" Width="100" />
                    
                    <TextBox Width="250" 
                             Text="{Binding UserName, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,5,5,5"/>
                    
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource TitleTextBlock}" 
                           Text="Password" Margin="0,5,5,5" Width="100" />

                    <PasswordBox Name="txtPassword" PreviewKeyUp="TxtPassword_OnPreviewKeyUp"  Margin="0,5,5,5" Width="250" />
                </StackPanel>

                <Button Command="{Binding StartCommand}" 
                        HorizontalAlignment="Left" 
                        Margin="105,5,0,0" 
                        Width="250"
                        Content="Start" />
            </StackPanel>
        </Grid>
       
        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"/>
        <ListBox Grid.Row="2" Name="lbLogs" />
    </Grid>
</UserControl>
